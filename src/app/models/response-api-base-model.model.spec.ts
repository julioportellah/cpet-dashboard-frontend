import { ResponseApiBaseModel } from './response-api-base-model.model';

describe('ResponseApiBaseModel', () => {
  it('should create an instance', () => {
    expect(new ResponseApiBaseModel()).toBeTruthy();
  });
});
