import { PatientFormResult } from './patient-form-result.model';

describe('PatientFormResult', () => {
  it('should create an instance', () => {
    expect(new PatientFormResult()).toBeTruthy();
  });
});
